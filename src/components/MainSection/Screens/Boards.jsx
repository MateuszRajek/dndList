import Button from "../../Button/Button";

function Boards() {
  return (
    <div>
      <Button>
        <p className="add-card-btn">+ Add a card</p>
      </Button>
    </div>
  );
}

export default Boards;
